<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="AdoteUmFocinhoMobile.Views.MasterPage"
             Title="Adote um Focinho">

    <MasterDetailPage.Master>
        <ContentPage Title="Menu">
            <StackLayout Padding="0,20,0,0">
                <Button Text="Principal"
                        Command="{Binding FeedCommand}"/>
                <Button Text="Meus Focinhos"
                        Command="{Binding MyFeedCommand}"/>
                <Button Text="Focinhos Favoritos"
                        Command="{Binding MyFavoritesCommand}"/>
                <Button Text="Sobre"/>
                <Button Text="Sair"/>
            </StackLayout>
        </ContentPage>
    </MasterDetailPage.Master>
  
</MasterDetailPage>